# Machine-specific state (absolute paths, auto-generated)
plugins/installed_plugins.json
plugins/known_marketplaces.json
plugins/install-counts-cache.json
plugins/cache/
plugins/marketplaces/
plugins/data/

# Sessions & runtime
sessions/
session-env/
shell-snapshots/
cache/
backups/
.omc/

# Transient files
history.jsonl
.session-stats.json
mcp-needs-auth-cache.json
plans/

# Project session logs (keep memory/, ignore session jsonl)
projects/*/*.jsonl
projects/*/[0-9a-f]*-[0-9a-f]*-*/

# Per-bot / per-project session state dirs (오직 -home-sungw-agents/memory/ 만 tracked)
projects/-home-sungw-agents-*/
projects/-home-sungw/

# Runtime counter state (rules/ 하위 auto-updated, commit 금지)
rules/counters.json
rules/counters.json.lock

# Transient cache / task state dirs
paste-cache/
tasks/

# Machine-specific settings (permissions, etc.)
settings.local.json

# Server-fetched blocklist
plugins/blocklist.json

# Credentials
.credentials.json
channels/discord/.env

# File history (machine-specific runtime state)
file-history/

# OS
.DS_Store
